apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafpvc
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: standard
  volumeName: grafpv
