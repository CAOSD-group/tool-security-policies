apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prompvc
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
  storageClassName: standard
  volumeName: prompv
