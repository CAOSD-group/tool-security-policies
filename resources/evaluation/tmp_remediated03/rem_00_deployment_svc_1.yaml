apiVersion: v1
kind: Service
metadata:
  name: production
  labels:
    app: production
spec:
  ports:
    - port: 32000
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: production
